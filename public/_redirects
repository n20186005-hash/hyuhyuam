# 入口与语言根路径统一为「带尾斜杠」形式。
# 站点以目录形式产出（/en/index.html 对外即 /en/），
# 若 /en 与 /en/ 同时可访问，搜索引擎会把两者分别收录、分散权重。
# 全部使用 301，便于搜索权重合并（原先 / → /ko/ 用的是 302，不会传递权重）。
/ /ko/ 301
/en /en/ 301
/zh /zh/ 301
/ja /ja/ 301
/ko /ko/ 301
